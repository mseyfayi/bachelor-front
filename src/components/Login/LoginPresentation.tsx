import classes from './LoginPresentation.module.scss';

type Props = {};

const LoginPresentation = (props: Props) => <div className={classes.container}>Login</div>;

export default LoginPresentation;
