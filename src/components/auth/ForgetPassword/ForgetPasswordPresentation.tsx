import classes from './ForgetPasswordPresentation.module.scss';

type Props = {};

const ForgetPasswordPresentation = ({}: Props) => <div className={classes.container} />;

export default ForgetPasswordPresentation;
