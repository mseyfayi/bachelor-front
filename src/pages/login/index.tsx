import Head from 'next/head';
import { NextPage } from 'next';

const LoginPage: NextPage = () => (
  <>
    <Head>
      <title>Login</title>
    </Head>
    <div />
  </>
);

export default LoginPage;
